# Cloudflare API Token
# Get your API token from: https://dash.cloudflare.com/profile/api-tokens
# Create a CUSTOM token with comprehensive permissions
# See docs/CLOUDFLARE_API_TOKEN_PERMISSIONS.md for complete permission list
# After creating token, you'll see a summary with all granted permissions
# Note the TTL (start/end dates) for when the token expires
# Test your token:
#   curl "https://api.cloudflare.com/client/v4/accounts/YOUR_ACCOUNT_ID/tokens/verify" -H "Authorization: Bearer YOUR_TOKEN"
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id_here
CLOUDFLARE_S3_API=https://your_account_id.r2.cloudflarestorage.com

# FalkorDB Configuration
# GraphMind uses FalkorDB via HTTP/REST API (NOT direct Redis protocol)
# REST API wrapper: scripts/falkordb-rest-api.js

# LOCAL DEVELOPMENT (default):
# Uses local Docker container + REST API wrapper on port 3001
FALKORDB_HOST=localhost
FALKORDB_PORT=6380
FALKORDB_USER=default
FALKORDB_PASSWORD=

# PRODUCTION (Cloudflare Tunnel):
# Uses Cloudflare Tunnel to expose local FalkorDB to production Workers
# See docs/FALKORDB_TUNNEL.md for complete tunnel setup instructions
# FALKORDB_HOST=falkordb-tunnel.aiwithapex.workers.dev.aiwithapex.com
# Note: In production, FALKORDB_HOST is set as a Cloudflare Secret
# Run: echo "your-tunnel-hostname" | npx wrangler secret put FALKORDB_HOST

# ALTERNATIVE: FalkorDB Cloud
# Sign up at: https://app.falkordb.cloud/
# FALKORDB_HOST=your-instance.falkordb.cloud
# FALKORDB_PORT=6379
# FALKORDB_USER=your_user
# FALKORDB_PASSWORD=your_password

# ALTERNATIVE: Self-Hosted VPS
# FALKORDB_HOST=your-vps-ip
# FALKORDB_PORT=6379
# FALKORDB_USER=default
# FALKORDB_PASSWORD=your_password

# Environment
NODE_ENV=development

# Authentication Configuration
# JWT_SECRET: Secret key for signing JWT tokens (256-bit random string)
# Generate with: openssl rand -base64 32
# IMPORTANT: Use different secrets for development vs production
JWT_SECRET=your_jwt_secret_here

# BCRYPT_COST: bcrypt hashing cost factor (10-14)
# Cost 12 = ~200ms hashing time (recommended for production)
# Cost 10 = ~50ms (can use for faster local development)
BCRYPT_COST=12

# Answer Generation Configuration (Feature 009)
# ANSWER_CACHE_TTL: Cache duration for generated answers in seconds
# Default: 3600 (1 hour)
ANSWER_CACHE_TTL=3600

# ANSWER_MAX_TOKENS: Maximum tokens for LLM answer generation
# Default: 200 (approximately 2-3 sentences)
ANSWER_MAX_TOKENS=200

# LLM_TEMPERATURE: Temperature for answer generation (0.0-1.0)
# 0.7 = balanced creativity and accuracy
# Lower = more factual, Higher = more creative
LLM_TEMPERATURE=0.7
